name: Keep App Awake

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping app
        run: |
          curl -I https://amsmigration-fpnbawzeu2yzwcfpybflrh.streamlit.app/
